# yuleCommunity API — 环境变量配置
# 复制此文件为 .env 并填入实际值

# 数据库密码（docker-compose 使用此值）
DB_PASSWORD=yulecommunity

# JWT 密钥（⚠️ 生产环境必须修改为随机字符串！）
# 生成: openssl rand -hex 32
JWT_SECRET=change-me-to-a-random-string

# API 端口（默认 3000）
API_PORT=3000

# 前端源码中可能需要后端地址
VITE_API_BASE_URL=http://localhost:3000
